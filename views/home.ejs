<%- include('partials/header') -%> 

<h1>Home</h1>
<p><%= homeStartingContent %> </p>

<% posts.forEach(element => { %>
    <h1><%= element.title %></h1>
    <% let body = utility.truncString(element.body) %>
    <p><%=body%> <a href="/posts/<%= element._id %>">Read More</a></p>
<%  }); %> 

<%- include('partials/footer') -%>